# 一、文档流

浏览器在渲染页面时，遵照从上往下、从左往右的顺序，依次排列，这种页面的排版方式就是文档流。

# 二、浮动布局
## 1. 什么是浮动布局
在 CSS 中，浮动（float）属性的出现是为了实现浮动元素周围文字环绕的效果。在网页设计中，如图中所示，应用了 float 属性的页面元素就像印刷布局里面被文字包围的图片一样。

```css
    img {
        float: left;    /* 图片向左浮动 */
    }
```

<p align=center><img src="https://p0-xtjj-private.juejin.cn/tos-cn-i-73owjymdk6/9ae43df5a49c4a2ebdc5baac5780e343~tplv-73owjymdk6-jj-mark-v1:0:0:0:0:5o6Y6YeR5oqA5pyv56S-5Yy6IEAg5p-aeXV6dW1p:q75.awebp?policy=eyJ2bSI6MywidWlkIjoiMzY2MDcxMjEyMzU3NjMzMSJ9&rk3s=e9ecf3d6&x-orig-authkey=f32326d3454f2ac7e96d3d06cdbb035152127018&x-orig-expires=1789178358&x-orig-sign=3fyPfkhjRmB0%2B8Uyh7CnRkUEIhE%3D" alt="image.png"  /></p>

## 2. 浮动的两大用途
**(1) 实现文字环绕**

如上，图片向左浮动后，文字会在图片右侧排列，超过图片高度后恢复正常排列。

浮动元素会脱离正常文档流，但仍会影响文字排版，让文字围绕它排列。

**(2) 实现元素水平排列**

给多个子元素设置 `float: left;`，就可以让它们从左向右并排排列，空间不足时换行。

```html
<ul class="list">
    <li>1</li>
    <li>2</li>
    <li>3</li>
</ul>
```

```css
.list {
    margin: 0;
    padding: 0;
    list-style: none;
}

.list li {
    float: left;
    width: 200px;
    height: 100px;
}

.list li:nth-child(1) { background-color: #d82814; }
.list li:nth-child(2) { background-color: #0c64f0; }
.list li:nth-child(3) { background-color: #b213d6; }
```

常用值有 `left`、`right` 和 `none`。`top`、`bottom` 是无效值，对应声明会被浏览器忽略。

![image.png](https://p0-xtjj-private.juejin.cn/tos-cn-i-73owjymdk6/ab05027992c34ebebfc0585c8d8baf4c~tplv-73owjymdk6-jj-mark-v1:0:0:0:0:5o6Y6YeR5oqA5pyv56S-5Yy6IEAg5p-aeXV6dW1p:q75.awebp?policy=eyJ2bSI6MywidWlkIjoiMzY2MDcxMjEyMzU3NjMzMSJ9&rk3s=e9ecf3d6&x-orig-authkey=f32326d3454f2ac7e96d3d06cdbb035152127018&x-orig-expires=1789179961&x-orig-sign=VrpdAxrqawJIko7PfcV5am8BUng%3D)


## 3. 如何清除浮动的副作用

当普通父容器没有设置高度，内部子元素又全部浮动时，子元素无法撑起父容器，导致**高度塌陷**，进而影响后续元素的排版。

![image.png](https://p0-xtjj-private.juejin.cn/tos-cn-i-73owjymdk6/5d16db30204442d986911dda5af1c15a~tplv-73owjymdk6-jj-mark-v1:0:0:0:0:5o6Y6YeR5oqA5pyv56S-5Yy6IEAg5p-aeXV6dW1p:q75.awebp?policy=eyJ2bSI6MywidWlkIjoiMzY2MDcxMjEyMzU3NjMzMSJ9&rk3s=e9ecf3d6&x-orig-authkey=f32326d3454f2ac7e96d3d06cdbb035152127018&x-orig-expires=1789180057&x-orig-sign=6r83MOkvd%2Ff3Za899aLrYVYAd8Q%3D)


常见的处理方式有以下五种，按需选择即可：

**(1) 直接给父容器设置高度 —— 不推荐**

```css
.list {
    height: 100px;
}
```

子元素换行或高度变化后，固定高度就可能不够用。

**(2) 在浮动元素末尾增加空容器 —— 不推荐**

在父容器内部、所有浮动元素之后，添加一个空的块级元素：

```html
<div style="clear: both;"></div>
```

`clear: both;` 让它排到前面左右浮动元素的下方，从而撑起父容器，但会增加无意义的标签。注意：`ul` 内不能直接放 `div`。

**(3) 为父容器设置 after 伪元素 —— 推荐**

```css
.list::after {
    content: "";
    display: block;
    clear: both;
}
```

原理与空容器相同，用伪元素代替真实标签，不需要修改 HTML 结构。

**(4) 给被影响的后续元素设置 clear**

```css
h2 {
    clear: both;
}
```

可以让后续标题避开浮动，但不能解决前面父容器的高度塌陷。

**(5) 为父容器设置 overflow: hidden**

```css
.list {
    overflow: hidden;
}
```

通过创建 BFC，让父容器计算高度时包含浮动子元素。不过，它也会裁剪溢出内容；如果只需要创建 BFC，可以使用 `display: flow-root;`。

# 三、BFC容器
## 1. 什么是BFC容器

BFC（Block Formatting Context），即**块级格式化上下文**，可以理解为一个拥有独立排版规则的区域。

例如，父子元素的 `margin-top` 在满足合并条件时，不会相加，而是发生外边距合并：

```css
.parent {
    margin-top: 100px;
    background-color: #4821d2;
}

.child {
    margin-top: 50px;
    height: 200px;
    background-color: #dc1c62;
}
```

当 `.child` 是第一个正常流子元素，父容器没有上边框、上内边距等阻隔时，这两个正外边距会合并为 `100px`，子元素不会在父容器内部留出 `50px` 的顶部间距。

给父容器设置 `overflow: hidden;` 或 `display: flow-root;`，创建 BFC 后，就可以阻止这里的父子外边距合并。

## 2. BFC容器的渲染规则

1. 在常见水平书写模式下，内部普通块级盒子从上往下排列。
2. 内部浮动不会影响容器外部的文字排版，但容器尺寸变化仍会影响外部布局。
3. 计算自动高度时，会将内部浮动元素也计算在内。
4. 容器的外边距不会与内部子元素的外边距合并，但内部相邻普通块级兄弟元素仍可能发生外边距合并。
5. 正常流中建立 BFC 的块盒会避开同一外部 BFC 中的浮动元素。

## 3. 如何创建BFC容器

常见方式：

1. `display: flow-root;` —— 直接创建 BFC。
2. 普通块容器设置 `overflow: hidden / auto / scroll;`。
3. `position: absolute / fixed;`。
4. `float: left / right;`。
5. `display: inline-block;`。

注意：`flex`、`grid` 创建的是各自的格式化上下文，不应直接称为 BFC。只为包含浮动时，使用 `flow-root` 更直接。
